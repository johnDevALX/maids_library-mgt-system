spring:
  application:
    name: maids_library-management-system
  profiles:
    active: local

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: false
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

#Provide config for postgres db connection, redis host and jwt secret